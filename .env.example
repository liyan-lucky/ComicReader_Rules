# Optional search provider keys for local rule discovery.
# Do not commit real secrets.

# Free self-hosted SearXNG (recommended): http://localhost:8080
# Or set in config/search.json "searxng.url"
# CI: set GitHub Secret SEARXNG_URL to your self-hosted instance
SEARXNG_URL=
# Paid APIs (limited quota, used as fallback)
BRAVE_SEARCH_API_KEY=
GOOGLE_API_KEY=
GOOGLE_CX=
SERPER_API_KEY=
