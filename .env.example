# Optional search provider keys for local rule discovery.
# Do not commit real secrets.

# Free self-hosted (recommended for CI): http://localhost:8080
SEARXNG_URL=
# Paid APIs (limited quota, used as fallback)
BRAVE_SEARCH_API_KEY=
GOOGLE_API_KEY=
GOOGLE_CX=
SERPER_API_KEY=
