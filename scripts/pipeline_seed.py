"""The complete fixed semantic seed set used by the discovery pipeline."""

ROOT_TERMS = ("漫画", "漫书")
# Compatibility for display-only call sites. Search code must use ROOT_TERMS.
ROOT_TERM = ROOT_TERMS[0]
